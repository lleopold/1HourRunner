.preload-root {
    width: 100%;
    height: 100%;
}
.preload-loading {
    position: absolute;
    right: 24px;
    bottom: 18px;
    font-size: 16px;
    color: rgba(255,255,255,0.92);
    -unity-font-style: bold;
    text-shadow: 0 1px 2px rgba(0,0,0,0.6);
}
